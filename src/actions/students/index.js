import fetchStudent, { FETCHED_STUDENTS } from './fetch'
import createStudent, { CREATE_STUDENT } from './create'
import updateStudent, { UPDATE_STUDENT } from './update'
import fetchOneStudent, { FETCHED_ONE_STUDENT } from './fetch'
import pickColor, {PICK_COLOR}  from './pickColor'
export {
  FETCHED_STUDENTS,
  CREATE_STUDENT,
  UPDATE_STUDENT,
  FETCHED_ONE_STUDENT,
  PICK_COLOR,
  pickColor,
  createStudent,
  updateStudent,
  fetchOneStudent,
}
